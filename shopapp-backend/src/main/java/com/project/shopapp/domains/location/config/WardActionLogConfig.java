package com.project.shopapp.domains.location.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WardActionLogConfig {
    // TODO: Khởi tạo Bean SlackNotifierClient hoặc KafkaProducer
    // chuyên dụng để handle các Event mang tính rủi ro cao (như ngưng giao hàng hàng loạt).
}