package com.project.shopapp.domains.sales.validation;
/**
 * TODO: Template.
 * Bảng này được sinh tự động bởi hệ thống, không nhận JSON input trực tiếp từ FE,
 * nên việc tạo Custom Validator là chưa cần thiết ở giai đoạn này.
 * Các rule check (như productItemId != null thì qty = 1) đã được cover bởi CHECK constraint ở DB.
 */
public class ValidOrderDetailLogic {}