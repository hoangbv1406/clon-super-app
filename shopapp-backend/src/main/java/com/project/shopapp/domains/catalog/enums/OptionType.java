package com.project.shopapp.domains.catalog.enums;

public enum OptionType {
    TEXT,       // Hiển thị dạng chữ (VD: 64GB, Size M)
    COLOR_HEX,  // Hiển thị mã màu (VD: #FF0000)
    IMAGE       // Hiển thị bằng icon/ảnh (VD: option hoa văn áo)
}