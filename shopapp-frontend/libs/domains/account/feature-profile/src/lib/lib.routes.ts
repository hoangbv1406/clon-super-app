import { Route } from '@angular/router';
import { AccountFeatureProfile } from './account-feature-profile/account-feature-profile';

export const accountFeatureProfileRoutes: Route[] = [
  { path: '', component: AccountFeatureProfile },
];
